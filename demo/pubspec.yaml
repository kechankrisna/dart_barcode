name: example
description: BarcodeWidget example
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  barcode_image:
  barcode_widget:
  file_selector:
  flutter:
    sdk: flutter
  google_fonts:
  image: ^4.0.0
  path_provider:
  pdf_ce:

dependency_overrides:
  barcode_image:
    path: ../image
  barcode_plus:
    path: ../barcode
  barcode_widget:
    path: ../flutter

dev_dependencies:
  flutter_lints:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
